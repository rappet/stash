#apiVersion: cert-manager.io/v1
#kind: Certificate
#metadata:
#  name: hello-world
#  namespace: traefik
#spec:
#  secretName: ingress-tls       # <===  Name of secret where the generated certificate will be stored.
#  dnsNames:
#    - {{ .Values.domain }}
#  issuerRef:
#    name: letsencrypt-prod
#    kind: Issuer